# ============================================================
#  Java Vulnerability Detection LLM — QLoRA Fine-Tuning Stack
#  Python 3.10+  |  CUDA 11.8+ / 12.1+
# ============================================================

# --- Core ML / DL ---
torch>=2.2.0
torchvision>=0.17.0

# --- Hugging Face ecosystem ---
transformers>=4.40.0
datasets>=2.18.0
tokenizers>=0.15.2
accelerate>=0.29.0
evaluate>=0.4.1

# --- PEFT / LoRA ---
peft>=0.10.0

# --- 4-bit quantisation (bitsandbytes) ---
# Install the CUDA-compatible wheel manually if this fails:
#   pip install bitsandbytes --index-url https://huggingface.github.io/bitsandbytes
bitsandbytes>=0.43.0

# --- Data processing ---
numpy>=1.26.0
scipy>=1.12.0

# --- Utilities ---
tqdm>=4.66.0
rich>=13.7.0        # Pretty CLI output
safetensors>=0.4.2  # Fast / safe model serialisation

# --- Optional: experiment tracking ---
# wandb>=0.16.0
# tensorboard>=2.16.0
